# validemaildetector
False Email Detector is a Java-based program that checks email addresses for valid formatting and suspicious patterns. It identifies disposable email domains, unusual dot patterns, excessive digits, and length issues, then classifies emails as INVALID, SUSPICIOUS, LIKELY VALID, or VALID FORMAT.
